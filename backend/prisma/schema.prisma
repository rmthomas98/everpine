// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

// Looking for ways to speed up your queries, or scale easily with your serverless or edge functions?
// Try Prisma Accelerate: https://pris.ly/cli/accelerate-init

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

enum SubscriptionStatus {
  ACTIVE
  INACTIVE
  PAST_DUE
  NEW_USER
  TRIAL
}

enum Role {
  SUPER_ADMIN // team owner, CRUD, change subscription, change team settings (ownly can be one)
  ADMIN // can do everything, create, read, update, delete, (can be multiple)
  USER // can only read and print data (can be multiple)
}

model User {
  id                     String             @id @default(cuid())
  createdAt              DateTime           @default(now())
  updatedAt              DateTime           @updatedAt
  name                   String?
  email                  String             @unique
  password               String
  role                   Role               @default(SUPER_ADMIN)
  isEmailVerified        Boolean            @default(false)
  emailVerificationToken String?            @unique
  trialEndDate           Int? // timestamp
  hasPaymentMethod       Boolean            @default(false)
  stripePaymentMethodId  String?
  stripeCustomerId       String?
  stripeSubscriptionId   String?
  subscriptionStatus     SubscriptionStatus @default(NEW_USER)
  latestInvoiceId        String? // get from payment failed webhook so when user updates payment method, we can retry the payment
  cancelAtPeriodEnd      Boolean            @default(false)
}
